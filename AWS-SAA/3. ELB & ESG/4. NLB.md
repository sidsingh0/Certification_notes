Layer 4 (Transport Layer)
Really high performance, millions of request per second
One static IP per AZ, also allows using Elastic IP
If exam says -> Can only be accessed with 1-3 different IPs, we think of NLB
Health check is supported on HTTP, HTTPS and TCP protocols
NOTE: When using NLB for instances, make sure that the target group protocol is TCP and NOT HTTP, for health check however, we can use HTTP
### Target Groups for NLB
1. EC2 Instances
2. Private IPs -> They allow on premise server to be load balanced
3. ALB 