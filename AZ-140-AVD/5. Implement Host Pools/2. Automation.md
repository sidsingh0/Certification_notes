- Set IE enchanced configuration to OFF
- In powershell, go to ise mode(for creating scripts)
- Set execution policy to unrestricted(so that scripts can run)
- Run New-AzWvdHostPool command with the params
- We can do this in Azure CLI too

We can also use ARM Templates from Azure Resource Manager. Everything works in JSON.
We can use export template from resource group

___

# Settings
- We can go to AVD > Scaling Plans > Create
- We can add exclusion tags for VMs we dont want to include in auto scaling process.
- Schedule has 4 phases
	- Ramp up 
	- Peak: We can force users to logoff and notify them N minutes before logoff
	- Ramp down
	- Off peak hours
- In order to assign scaling plan, all the machines need to be shut down
For session host settings, just take a look at lecture 51
___

Apply window license to a session host VMs
1. If we create a host pool and session hosts inside, license is applied automatically
2. Create host pool using GIthub or ARM templates, license is applied automatically
3. **If we create host VMs using powershell or CLI, license needs to be applied manually through a command**