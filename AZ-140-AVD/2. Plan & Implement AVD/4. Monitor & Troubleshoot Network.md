- Log analytics workspace saves logs from different services.
- Always check if we can connect to the windows machine using RDP.
- Whenever a vNET is created, a network watcher is also created. One is created for every region.
	- Helps monitor and troubleshoot network issues.
	- **IP Flow Verify Feature** lets us verify if certain requests pass through or not.
	- **Next Hop** allows us to find the next hop of the packet. It allows us to make sure if a firewall, NAT is attached in the network.
	- **Packet Capture Tool** is like Wireshark
	- **Connection Troubleshooter** from one VM to another
- Resource > **Metrics** allow us to view performance metrics related to the appliance
- Diagnostic settings allow us to capture the log data for what is going on in the machine. It can send logs to log analytics workplace.
- **Azure Subscription > Cost Analysis** 